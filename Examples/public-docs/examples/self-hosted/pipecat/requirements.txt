# bitHuman Pipecat Avatar Agent — supports both Daily.co and LiveKit transports

# Pipecat framework with transport and service integrations
pipecat-ai[daily,livekit,openai,deepgram]>=0.0.40

# LiveKit SDK — token generation for agent_pipecat_livekit.py
livekit>=0.11.0

# bitHuman avatar runtime
bithuman>=1.6.0

# Environment, HTTP, image, and audio utilities
python-dotenv>=1.1.1
aiohttp>=3.9.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
